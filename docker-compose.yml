# =============================================================================
# docker-compose.yml — local development only (not for production)
# =============================================================================

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - ENVIRONMENT=development
      - ENV=development
    volumes:
      - ./ingestion/data/indexes:/app/ingestion/data/indexes:ro
      - ./ingestion/data/raw:/app/ingestion/data/raw:ro
      - ./ingestion/data/cleaned:/app/ingestion/data/cleaned:ro
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 15s
